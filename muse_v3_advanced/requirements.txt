# MUSE v3 Advanced System Requirements
# ====================================

# Core ML and Deep Learning
torch>=1.12.0
torchvision>=0.13.0
torchaudio>=0.12.0
transformers>=4.20.0
sentence-transformers>=2.2.0
clip-by-openai>=1.0.0

# Data Processing and Analysis
numpy>=1.21.0
pandas>=1.4.0
scikit-learn>=1.1.0
scipy>=1.8.0

# Natural Language Processing
nltk>=3.7.0
spacy>=3.4.0
langdetect>=1.0.9
polyglot>=16.7.4

# Computer Vision
opencv-python>=4.5.0
Pillow>=9.0.0
matplotlib>=3.5.0

# Web and API
requests>=2.27.0
fastapi>=0.75.0
uvicorn>=0.17.0
aiohttp>=3.8.0

# Database and Storage
sqlite3  # Built-in with Python
pymongo>=4.1.0  # Optional for MongoDB
redis>=4.2.0  # Optional for caching

# Development and Testing
pytest>=7.1.0
pytest-asyncio>=0.18.0
black>=22.3.0
flake8>=4.0.0
mypy>=0.950

# Logging and Monitoring
loguru>=0.6.0
wandb>=0.12.0  # Optional for experiment tracking
tensorboard>=2.8.0  # Optional for visualization

# Utilities
tqdm>=4.64.0
click>=8.1.0
python-dotenv>=0.20.0
pydantic>=1.9.0

# Hindi Language Support
indic-transliteration>=2.3.0
polyglot>=16.7.4
icu>=2.0  # For advanced text processing

# Optional: GPU Acceleration
# nvidia-ml-py3>=7.352.0  # For NVIDIA GPU monitoring

# Optional: Advanced ML
# faiss-gpu>=1.7.0  # For efficient similarity search
# chromadb>=0.3.0  # For vector database

# System and Performance
psutil>=5.9.0
memory-profiler>=0.60.0

# Configuration and Environment
pyyaml>=6.0
toml>=0.10.2
configparser>=5.2.0

# Async and Concurrency
asyncio  # Built-in with Python 3.7+
concurrent.futures  # Built-in
multiprocessing  # Built-in

# Data Validation and Types
typing-extensions>=4.2.0
dataclasses>=0.6  # Built-in with Python 3.7+
enum34>=1.1.10; python_version < '3.4'

# File and Path Handling
pathlib  # Built-in with Python 3.4+
os  # Built-in
json  # Built-in
pickle  # Built-in

# Math and Statistics
random  # Built-in
math  # Built-in
statistics  # Built-in

# Time and Date
time  # Built-in
datetime  # Built-in

# Regular Expressions
re  # Built-in

# Warning: Some dependencies may require system-level installations
# 
# For Hindi language support on Ubuntu/Debian:
# sudo apt-get install libicu-dev
# sudo apt-get install python3-icu
#
# For CUDA support (if using GPU):
# Install CUDA toolkit from NVIDIA
# pip install torch torchvision torchaudio --extra-index-url https://download.pytorch.org/whl/cu117
#
# For development setup:
# pip install -r requirements.txt
# python -m spacy download en_core_web_sm
# python -m spacy download xx_core_web_sm  # Multi-language model
